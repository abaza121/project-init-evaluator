[package]
name = "project-initiation-evaluator"
version = "0.1.0"
edition = "2024"
publish = false
description = "Independent evaluator for generated project-foundation packages"
license = "MIT"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
